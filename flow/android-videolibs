if [ ! -z "${ANDROIDVIDEOLIBS}" ]
then
    SOURCE=sources/android-videolibs-9.0
    echo "Building $SOURCE ..." >> $LOG
    . $CWD/scripts/android-videolibs; package_make android-videolibs 9.0 tw1
fi
